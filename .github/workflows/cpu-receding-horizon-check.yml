name: CPU receding horizon check

on:
  push:
    branches:
      - cpu-search-behaviour-fixes

permissions:
  contents: write

jobs:
  patch-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: cpu-search-behaviour-fixes
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'
      - name: Apply planning improvements
        run: python tools/apply_receding_horizon_and_purchase_clustering.py
      - name: Restore
        run: dotnet restore CrownAndSiege.sln
      - name: Test
        run: dotnet test CrownAndSiege.sln --configuration Release --no-restore
      - name: Commit verified improvements
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add MedivalChess.CPU/CpuSearch.cs MedivalChess.CPU/CpuActionGenerator.cs MedivalChess.CPU/CpuActionCandidates.cs MedivalChess.Tests/CpuPlanningEfficiencyTests.cs
          git diff --cached --quiet && exit 0
          git commit -m "Improve CPU receding-horizon planning and purchase placement"
          git push origin HEAD:cpu-search-behaviour-fixes
